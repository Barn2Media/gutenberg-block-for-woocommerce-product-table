"use strict";

function _createForOfIteratorHelper(o) { if (typeof Symbol === "undefined" || o[Symbol.iterator] == null) { if (Array.isArray(o) || (o = _unsupportedIterableToArray(o))) { var i = 0; var F = function F() {}; return { s: F, n: function n() { if (i >= o.length) return { done: true }; return { done: false, value: o[i++] }; }, e: function e(_e) { throw _e; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var it, normalCompletion = true, didErr = false, err; return { s: function s() { it = o[Symbol.iterator](); }, n: function n() { var step = it.next(); normalCompletion = step.done; return step; }, e: function e(_e2) { didErr = true; err = _e2; }, f: function f() { try { if (!normalCompletion && it.return != null) it.return(); } finally { if (didErr) throw err; } } }; }

function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(n); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }

function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) { arr2[i] = arr[i]; } return arr2; }

(function (wp, nonce, data) {
  "use strict";

  var __ = wp.i18n.__;
  var createElement = wp.element.createElement;
  var _wp$components = wp.components,
      Button = _wp$components.Button,
      Icon = _wp$components.Icon,
      IconButton = _wp$components.IconButton,
      ToggleControl = _wp$components.ToggleControl;
  var withState = wp.compose.withState;
  var el = createElement;

  if (!window.productTableBlockComponents) {
    window.productTableBlockComponents = {};
  }

  var filterSelectionOptions = {
    'category': {
      label: 'Category',
      andor: true,
      for: 'category',
      multiple: true
    },
    'tag': {
      label: 'Tag',
      andor: true,
      for: 'tag',
      multiple: true
    },
    'cf': {
      label: 'Custom Field',
      andor: true,
      keypair: ['Key', 'Value'],
      multiple: true,
      for: 'value'
    },
    'term': {
      label: 'Custom Taxonomy Term',
      keypair: ['Taxonomy', 'Term'],
      multiple: true,
      for: 'value'
    },
    'attr': {
      attr: 'term',
      label: 'Attribute',
      keypair: ['Attribute', 'Term'],
      for: 'attr',
      multiple: true
    },
    'year': {
      label: 'Year',
      for: 'value'
    },
    'month': {
      label: 'Month',
      for: 'value'
    },
    'day': {
      label: 'Day',
      for: 'value'
    },
    'status': {
      label: 'Product Status',
      for: 'status',
      values: ['publish', 'draft', 'private', 'pending', 'future', 'any'],
      multiple: true
    },
    'include': {
      label: 'Include Products',
      description: 'Product IDs',
      for: 'value'
    },
    'exclude': {
      label: 'Exclude Products',
      description: 'Product IDs',
      for: 'value'
    },
    'exclude_category': {
      andor: true,
      label: 'Exclude Category',
      for: 'category',
      multiple: true
    },
    'user_products': {
      label: 'Previously Ordered Products',
      value: 'true'
    } //'variations=separate': { label: 'Show Variations' },

  };

  var createProductSelectionFilters = function createProductSelectionFilters(_ref) {
    var filters = _ref.filters,
        onDelete = _ref.onDelete;
    var filterNodes = [];

    for (var i in filters) {
      var prettyValue = filters[i].value.replace(/,/g, ', ').replace(/\+/g, ' + ');
      var node = el('li', {
        'data-index': i,
        'data-key': filters[i].key,
        'data-value': filters[i].value,
        key: 'table-column-' + i
      }, [
      /*el(
      	Icon,
      	{
      		icon: barn2_reorderIcon,
      		alt: ''
      	}
      ),*/
      el('span', {}, [el('strong', {}, filterSelectionOptions[filters[i].key].label + ': '), prettyValue]), el(IconButton, {
        icon: barn2_deleteIcon,
        label: 'Remove Selection',
        'data-index': i,
        onClick: function onClick(e) {
          onDelete(e.currentTarget.dataset.index);
        }
      })]);
      filterNodes.push(node);
    }

    return filterNodes;
  };

  var getFilterSelectionOptions = function getFilterSelectionOptions() {
    var optionNodes = [];
    optionNodes.push(el('option', {
      value: ''
    }, __('Select products', 'wpt-block')));

    for (var key in filterSelectionOptions) {
      optionNodes.push(el('option', {
        value: key,
        'data-for': filterSelectionOptions[key].for,
        'data-key': filterSelectionOptions[key].attr || key,
        'data-multiple': filterSelectionOptions[key].multiple,
        'data-andor': filterSelectionOptions[key].andor,
        'data-description': filterSelectionOptions[key].description,
        'data-value': filterSelectionOptions[key].value
      }, filterSelectionOptions[key].label));
    }

    return optionNodes;
  };

  var getFilterSelectionOptionValues = function getFilterSelectionOptionValues(values, recursive) {
    var optionNodes = [];
    optionNodes.push(el('option', {
      value: ''
    }, __('Select value', 'wpt-block')));

    for (var key in values) {
      if (recursive && values[key].terms) {
        for (var subkey in values[key].terms) {
          optionNodes.push(el('option', {
            value: key + ':' + subkey,
            'data-key': key
          }, values[key].terms[subkey]));
        }
      } else {
        if (isNaN(key)) {
          optionNodes.push(el('option', {
            value: key
          }, values[key].label));
        } else {
          optionNodes.push(el('option', {
            value: values[key]
          }, values[key]));
        }
      }
    }

    return optionNodes;
  };

  var resetPanel = function resetPanel(panel) {
    panel.classList.remove('allow-multiple');
    panel.classList.remove('allow-andor');
    panel.classList.remove('has-multiple');
    var disabled = panel.querySelectorAll('*[disabled]');

    var _iterator = _createForOfIteratorHelper(disabled),
        _step;

    try {
      for (_iterator.s(); !(_step = _iterator.n()).done;) {
        var option = _step.value;
        option.disabled = false;
      }
    } catch (err) {
      _iterator.e(err);
    } finally {
      _iterator.f();
    }

    var selectors = panel.querySelectorAll('select,input');

    var _iterator2 = _createForOfIteratorHelper(selectors),
        _step2;

    try {
      for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
        var obj = _step2.value;
        obj.value = '';
        obj.classList.remove('visible');
        obj.classList.remove('selected');
        obj.classList.remove('ready');
      }
    } catch (err) {
      _iterator2.e(err);
    } finally {
      _iterator2.f();
    }

    panel.querySelector('ul').innerHTML = '';
  };

  var selectProductOption = function selectProductOption(e, panel) {
    var self = e.currentTarget;
    var value = self.value;
    var thisOption = self.querySelector("option[value=\"".concat(value, "\"]"));
    panel.classList.remove('allow-multiple');
    panel.classList.remove('has-multiple');
    panel.classList.remove('allow-andor');
    self.classList.remove('selected');
    var options = self.parentNode.querySelectorAll('.barn2-wc-product-table-block__new-option');

    var _iterator3 = _createForOfIteratorHelper(options),
        _step3;

    try {
      for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
        var option = _step3.value;
        option.classList.remove('visible');
        option.value = '';
        option.setAttribute('placeholder', '');
      }
    } catch (err) {
      _iterator3.e(err);
    } finally {
      _iterator3.f();
    }

    if (thisOption.dataset.for) {
      var selector = self.parentNode.querySelector('.barn2-wc-product-table-block__new-option.' + thisOption.dataset.for);
      selector.classList.add('visible');

      if (thisOption.dataset.multiple) {
        panel.classList.add('allow-multiple');
        var label = self.parentNode.querySelector('.barn2-wc-product-table-block__new-filter-selection-label');
        var button = self.parentNode.querySelector('.barn2-wc-product-table-block__add-filter-button');

        if (label) {
          label.innerHTML = thisOption.innerHTML + ' ' + __('Selections', 'wpt-block');
        }

        if (button) {
          button.innerHTML = __('Add', 'wpt-block') + ' ' + thisOption.innerHTML;
        }
      }

      if (thisOption.dataset.andor) {
        panel.classList.add('allow-andor');
      }

      if (thisOption.dataset.description) {
        selector.setAttribute('placeholder', thisOption.dataset.description);
      }
    } else {
      self.classList.add('selected');
    }
  };

  var selectProductKey = function selectProductKey(e) {
    var self = e.currentTarget;

    if (self.value === '') {
      self.classList.remove('selected');
    } else {
      self.classList.add('selected');
    }
  };

  var selectProductAttr = function selectProductAttr(e, modal) {
    var self = e.currentTarget;
    var attrValues = modal.querySelector('select.attr-values');

    if (self.value === '') {
      self.classList.remove('ready');
      attrValues.classList.remove('visible');
    } else {
      self.classList.add('ready');
      attrValues.classList.add('visible');
    }

    attrValues.value = '';
    var attrOptions = attrValues.querySelectorAll('option');

    var _iterator4 = _createForOfIteratorHelper(attrOptions),
        _step4;

    try {
      for (_iterator4.s(); !(_step4 = _iterator4.n()).done;) {
        var option = _step4.value;

        if (option.dataset.key === self.value) {
          option.style.display = '';
        } else {
          option.style.display = 'none';
        }
      }
    } catch (err) {
      _iterator4.e(err);
    } finally {
      _iterator4.f();
    }
  };

  var selectProductAttrValue = function selectProductAttrValue(e) {
    var self = e.currentTarget;

    if (self.value === '') {
      self.classList.remove('selected');
    } else {
      self.classList.add('selected');
    }
  };

  var selectProductValue = function selectProductValue(e) {
    var self = e.currentTarget;

    if (self.value === '') {
      self.classList.remove('selected');
    } else {
      self.classList.add('selected');
    }
  };

  var addFilterSelection = function addFilterSelection(panel) {
    var key = panel.querySelector('.barn2-wc-product-table-block__add-new-selection'),
        value = panel.querySelector('.barn2-wc-product-table-block__new-option.selected');
    key.disabled = true;
    var list = panel.querySelector('ul'),
        item = document.createElement('li');
    item.innerHTML = value.value;
    item.dataset.value = value.value;
    list.append(item);

    if (list.children.length > 1) {
      panel.classList.add('has-multiple');
    }

    key.disabled = true;
    value.value = '';
    value.classList.remove('selected');

    if (value.tagName === 'SELECT') {
      var option = value.querySelector("option[value=\"".concat(value.value, "\"]"));

      if (option) {
        option.disabled = true;
      }
    }
  };

  var getNewFilter = function getNewFilter(panel, matchAll) {
    var key = panel.querySelector('.barn2-wc-product-table-block__add-new-selection'),
        values = panel.querySelectorAll('ul li');
    var selectedOption = key.querySelector("option[value=\"".concat(key.value, "\"]"));
    var newFilterKey, newFilterValue;

    if (values.length) {
      newFilterKey = selectedOption.dataset.key;
      var joinChar = panel.classList.contains('allow-andor') && matchAll ? '+' : ',';
      var filters = [];

      var _iterator5 = _createForOfIteratorHelper(values),
          _step5;

      try {
        for (_iterator5.s(); !(_step5 = _iterator5.n()).done;) {
          var li = _step5.value;
          filters.push(li.dataset.value);
        }
      } catch (err) {
        _iterator5.e(err);
      } finally {
        _iterator5.f();
      }

      newFilterValue = filters.join(joinChar);
    } else if (!panel.classList.contains('allow-multiple')) {
      var value = panel.querySelector('.barn2-wc-product-table-block__new-option.selected');

      if (selectedOption.dataset.value && selectedOption.dataset.value.length > 0) {
        newFilterKey = key.value;
        newFilterValue = selectedOption.dataset.value;
      } else {
        var _value = panel.querySelector('.barn2-wc-product-table-block__new-option.selected');

        var split = key.value.split('=');
        newFilterKey = split[0];
        newFilterValue = split.length > 1 ? split[1] : '';

        if (split.length === 1) {
          newFilterValue = _value.value;
        }
      } //saveNewFilter( { key: newFilterKey, value: newFilterValue } );

    }

    return {
      key: newFilterKey,
      value: newFilterValue
    };
  };

  var getFiltersOrder = function getFiltersOrder(list) {
    var newColumnOrder = [];
    var columnsSelected = list.querySelectorAll('li');

    for (var i = 0; i < columnsSelected.length; i += 1) {
      newColumnOrder.push({
        key: columnsSelected[i].dataset.key,
        value: columnsSelected[i].dataset.value
      });
    }

    return newColumnOrder;
  };

  window.productTableBlockComponents.ProductSelection = withState({
    isMatchall: false,
    count: null
  })(function (_ref2) {
    var isMatchall = _ref2.isMatchall,
        count = _ref2.count,
        attributes = _ref2.attributes,
        saveFilters = _ref2.saveFilters,
        setState = _ref2.setState;
    wp.apiFetch.use(wp.apiFetch.createNonceMiddleware(nonce));
    wp.apiFetch({
      path: '/wc-product-table/v1/count',
      method: 'POST',
      data: {
        attrs: attributes
      }
    }).then(function (res) {
      if (count == null || res.count !== count) {
        setState({
          count: res.count
        });
      }
    });
    var filters = attributes.filters;
    var filterSelectionsRef = wp.element.createRef();
    var newFilterPanelRef = wp.element.createRef();

    var addFilter = function addFilter(filter) {
      var newFilters = JSON.parse(JSON.stringify(filters));
      newFilters.push(filter);
      resetPanel(newFilterPanelRef.current);
      saveFilters(newFilters);
    };

    if (count != null && count >= 100) {
      count = 'At least 100';
    }

    var productElements = [el('h3', {}, [__('Products', 'wpt-block'), el('em', {}, count != null ? "".concat(count, " products found") : 'Finding products...')]), el('ul', {
      className: 'barn2-wc-product-table-block__product-filters',
      ref: filterSelectionsRef
    }, createProductSelectionFilters({
      filters: filters,
      onDelete: function onDelete(index) {
        var newFilters = removeArrayIndex(filters, index);
        saveFilters(newFilters);
      }
    })), el('p', {
      className: 'empty-options'
    }, __('(Using global options)', 'wpt-block')), el('div', {
      className: 'barn2-wc-product-table-block__new-filter-panel',
      ref: newFilterPanelRef
    }, [el('select', {
      className: 'barn2-wc-product-table-block__add-new-selection',
      onChange: function onChange(e) {
        selectProductOption(e, newFilterPanelRef.current);
      }
    }, getFilterSelectionOptions()), el('strong', {
      className: 'barn2-wc-product-table-block__new-filter-selection-label'
    }, ''), el('select', {
      className: 'barn2-wc-product-table-block__new-option category',
      onChange: selectProductKey
    }, getFilterSelectionOptionValues(data.categoryTerms)), el('select', {
      className: 'barn2-wc-product-table-block__new-option status',
      onChange: selectProductKey
    }, getFilterSelectionOptionValues(filterSelectionOptions.status.values)), el('select', {
      className: 'barn2-wc-product-table-block__new-option tag',
      onChange: selectProductKey
    }, getFilterSelectionOptionValues(data.tagTerms)), el('select', {
      className: 'barn2-wc-product-table-block__new-option attr',
      onChange: function onChange(e) {
        selectProductAttr(e, newFilterPanelRef.current);
      }
    }, getFilterSelectionOptionValues(data.attributes)), el('select', {
      className: 'barn2-wc-product-table-block__new-option attr-values',
      onChange: selectProductAttrValue
    }, getFilterSelectionOptionValues(data.attributes, true)), el('input', {
      className: 'barn2-wc-product-table-block__new-option value',
      onChange: selectProductValue
    }), el(Button, {
      className: 'is-secondary barn2-wc-product-table-block__add-filter-button',
      onClick: function onClick(e) {
        addFilterSelection(newFilterPanelRef.current, addFilter);
      }
    }, __('Add', 'wpt-block')), el('ul', {
      className: 'barn2-wc-product-table-block__new-filter-selections'
    }), el(ToggleControl, {
      className: 'barn2-wc-product-table-block__andor-toggle',
      label: __('Products must match all values', 'wpt-block'),
      checked: isMatchall,
      onChange: function onChange() {
        setState({
          isMatchall: !isMatchall
        });
      }
    }), el(Button, {
      className: 'is-primary barn2-wc-product-table-block__save-filter-button',
      onClick: function onClick(e) {
        var newFilter = getNewFilter(newFilterPanelRef.current, isMatchall);
        addFilter(newFilter);
      }
    }, __('Add to Table', 'wpt-block')), el(Button, {
      className: 'is-secondary barn2-wc-product-table-block__save-filter-button',
      onClick: function onClick(e) {
        resetPanel(newFilterPanelRef.current);
      }
    }, __('Cancel', 'wpt-block'))])];
    /*waitForReference( filterSelectionsRef, ( ref ) => {
    	if ( ! ref.classList.contains( 'ui-sortable' ) ) {
    		let $sortRef = jQuery( ref );
    		$sortRef.sortable( {
    			update: function() {
    				let newFilters = getFiltersOrder( ref );
    				console.log( newFilters );
    				$sortRef.sortable( 'cancel' );
    				saveFilters( newFilters );
    			}
    		} );
    	}
    } );*/

    return el('div', {
      className: 'barn2-wc-product-table-block__products'
    }, productElements);
  });
})(window.wp, typeof wcptbNonce !== 'undefined' ? wcptbNonce : null, typeof wcptbCatalog !== 'undefined' ? wcptbCatalog : null);